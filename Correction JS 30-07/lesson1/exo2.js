// Déclaration des deux premières variables avec des nombres
var var1 = 8;
var var2 = 5;

// Déclaration de la troisième variable comme étant l'addition des deux premières
var var3 = var1 + var2;

// Affichage du résultat dans la console avec la phrase explicative
console.log("Le résultat de " + var1 + " + " + var2 + " est " + var3);

// Déclaration des deux variables initiales
var1 = 10;
var2 = 4;

// Soustraction des deux variables et stockage dans la troisième variable
var3 = var1 - var2;

// Affichage du résultat de la soustraction dans la console
console.log("Le résultat de " + var1 + " - " + var2 + " est " + var3);

// Déclaration des deux variables initiales
var1 = 6;
var2 = 7;

// Multiplication des deux variables et stockage dans la troisième variable
var3 = var1 * var2;

// Affichage du résultat de la multiplication dans la console
console.log("Le résultat de " + var1 + " * " + var2 + " est " + var3);

// Déclaration des deux variables initiales
var1 = 20;
var2 = 4;

// Division des deux variables et stockage dans la troisième variable
var3 = var1 / var2;

// Affichage du résultat de la division dans la console
console.log("Le résultat de " + var1 + " / " + var2 + " est " + var3);

// Déclaration des deux variables initiales
var1 = 15;
var2 = 4;

// Calcul du modulo des deux variables et stockage dans la troisième variable
var3 = var1 % var2;

// Affichage du résultat du modulo dans la console
console.log("Le résultat de " + var1 + " % " + var2 + " est " + var3);

// Déclaration de la première variable avec une lettre (string) et de la deuxième variable avec un nombre
var1 = "A";
var2 = 10;

// Addition des deux variables et stockage dans la troisième variable
var3 = var1 + var2;

// Affichage du résultat de l'addition dans la console
console.log("Le résultat de " + var1 + " + " + var2 + " est " + var3);
