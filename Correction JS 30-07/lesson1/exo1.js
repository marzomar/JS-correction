// Déclaration de la variable `toto` avec la valeur "bonjour"
var toto = "bonjour";
// Affichage de la valeur de `toto` dans la console
console.log(toto);

// Remplacement de la valeur de `toto` par le résultat d'une addition
toto = 5 + 3;
// Affichage de la nouvelle valeur de `toto` dans la console
console.log(toto);

// Remplacement de la valeur de `toto` par le résultat d'une soustraction
toto = 10 - 4;
// Affichage de la nouvelle valeur de `toto` dans la console
console.log(toto);

// Remplacement de la valeur de `toto` par le résultat d'une multiplication
toto = 6 * 7;
// Affichage de la nouvelle valeur de `toto` dans la console
console.log(toto);

// Remplacement de la valeur de `toto` par le résultat d'une division
toto = 20 / 4;
// Affichage de la nouvelle valeur de `toto` dans la console
console.log(toto);

// Remplacement de la valeur de `toto` par le résultat d'un modulo
toto = 15 % 4;
// Affichage de la nouvelle valeur de `toto` dans la console
console.log(toto);
