// Déclaration de la variable 'n' initialisée à 0
let n = 0; // départ

// Boucle 'while' pour afficher les nombres de 0 à 1000
while (n <= 1000) { // condition
    console.log(n); // Affiche la valeur de 'n' dans la console
    n++; // Incrémente 'n' de 1 à chaque itération // itération
}
